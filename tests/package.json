{
  "name": "vocalpure-tests",
  "private": true,
  "description": "Headless (jsdom) smoke tests for the VocalPure Android app UI and the download site",
  "scripts": {
    "test": "node app.test.js && node site.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
